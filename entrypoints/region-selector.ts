// entrypoints/region-selector.ts - Entry point for region selector
export default defineUnlistedScript(() => {
  // This script will be built as region-selector.js and loaded by the HTML page
  
  console.log('🎯 Region selector script loaded');
  
  // The RegionSelector class will be defined directly in the generated JS file
  // since we need it to work in a standalone browser context
  
  // Import is not needed here as the functionality is self-contained in the HTML page
  // The region-selector.js file will contain all the necessary code
});